bitcoin (optionally with ord),
litecoin (optionally with ord-litecoin),
ipfs (optionally with ipfs-cluster),
dogecoin,
ethereum (execution client + consensus client; mainnet and the Sepolia testnet),
ethereum-classic
